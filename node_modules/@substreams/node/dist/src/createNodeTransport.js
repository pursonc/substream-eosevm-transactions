import { createGrpcWebTransport } from "@connectrpc/connect-node";
import { createInterceptors } from "./interceptors.js";
export function createNodeTransport(baseUrl, token, registry, headers) {
    return createGrpcWebTransport({
        baseUrl,
        httpVersion: "2",
        interceptors: createInterceptors(token, headers),
        jsonOptions: {
            typeRegistry: registry,
        },
    });
}
export default createNodeTransport;
//# sourceMappingURL=createNodeTransport.js.map