import { createGrpcWebTransport } from "@connectrpc/connect-web";
import { createInterceptors } from "./interceptors.js";
export function createWebTransport(baseUrl, token, registry, headers) {
    return createGrpcWebTransport({
        baseUrl,
        interceptors: createInterceptors(token, headers),
        jsonOptions: {
            typeRegistry: registry,
        },
    });
}
export default createWebTransport;
//# sourceMappingURL=createWebTransport.js.map